package protodeco.example;

import "pets.proto";

message Person {
    required string name = 1;
    required int32 age = 2;

    repeated Person friends = 3;
    repeated Pets pets = 4;

    repeated string words = 5;
    optional string value = 6;
}
